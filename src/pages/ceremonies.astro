---
import { getEntry } from 'astro:content';
import TherapyPage from '../components/therapy/TherapyPage.astro';

const entry = await getEntry('therapies', 'ceremonies');
if (!entry) throw new Error('ceremonies entry not found');
---

<TherapyPage entry={entry} />
