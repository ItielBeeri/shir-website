---
import { getEntry } from 'astro:content';
import TherapyPage from '../components/therapy/TherapyPage.astro';

const entry = await getEntry('therapies', 'psychotherapy');
if (!entry) throw new Error('psychotherapy entry not found');
---

<TherapyPage entry={entry} />
