---
import { getEntry } from 'astro:content';
import TherapyPage from '../components/therapy/TherapyPage.astro';

const entry = await getEntry('therapies', 'workshops');
if (!entry) throw new Error('workshops entry not found');
---

<TherapyPage entry={entry} />
