---
interface Props {
  /** Delay in ms cascaded across nested [data-reveal] children. */
  stagger?: number;
  class?: string;
}

const { stagger, class: className } = Astro.props;
---

<div
  class:list={['scroll-reveal', className]}
  data-reveal
  data-stagger={stagger}
>
  <slot />
</div>
