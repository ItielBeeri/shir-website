---
const {
  hover = false,
  padding,
  class: className,
  as: Tag = 'div',
} = Astro.props;
---

<Tag
  class:list={['glass-card', hover && 'glass-card--hover', className]}
  style={padding ? `padding: ${padding}` : undefined}
>
  <slot />
</Tag>

